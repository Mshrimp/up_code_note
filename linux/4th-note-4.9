vim:

	任何一个C语言程序，都是从main函数开始，而且只能有一个main函数。
函数格式
函数返回类型：int

export a	 定义全局变量

[root@localhost etc]# pstree | grep bash
     |-gnome-terminal-+-3*[bash]
     |                |-bash-+-grep
     |                |-bash---gedit
     |-login---bash
	->以树的形式查看进程



gcc:

